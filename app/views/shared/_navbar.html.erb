<nav class="navbar navbar-dark bg-dark navbar-expand-lg">
  <div class="container">
    <a class="navbar-brand" href="<%= root_path %>">NF S</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="<%= restaurants_path %>">Restaurantes</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= devices_path %>">Dispositivos</a>
        </li>
      </ul>
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <span class="nav-link">Bienvenido/a: <%= current_user.email %></span>
          </li>
          <li class="nav-item">
             <%= button_to "Logout", destroy_user_session_path, class: "btn btn-outline-danger", method: :delete %>
          </li>
        <% else %>
          <li class="nav-item">
            <a class="nav-link active" href="<%= new_user_session_path %>">Inicia sesión</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="<%= new_user_registration_path %>">Registrate</a>
          </li>
        <% end %>
      </ul>
  </div>
</nav>